// The classic top Header is superseded by <AppShell /> (sidebar + top bar).
// Kept as a no-op export so existing imports across pages continue to work.
export function Header() {
  return null;
}

export default Header;
